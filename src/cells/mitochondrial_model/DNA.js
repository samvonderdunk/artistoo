

class DNA {

		/* eslint-disable */ 
	constructor (conf, parent) {
        this.quality = new Array(this.conf["N_OXPHOS"]).fill(1);
        if (parent instanceof DNA){
            this.quality = parent.quality
            if 
        }
       
    }

}

export default DNA